<nav class="navbar navbar-expand-lg bg-primary mb-5" data-bs-theme="dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home.index') }}">
            <strong>Symrecipe</strong>
        </a>
        <button class="navbar-toggler" 
                type="button" 
                data-bs-toggle="collapse" 
                data-bs-target="#navbarColor01" 
                aria-controls="navbarColor01" 
                aria-expanded="false" 
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">

            <ul class="navbar-nav me-auto">

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recipe.community') }}">Recette de la communauté</a>
                </li>

                <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact.index') }}">Contact</a>
                    </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('ingredient.index') }}">Mes ingrédients</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('recipe.index') }}">Mes recettes</a>
                    </li> 
                {% endif %}
            </ul> 

            <div class="d-flex">
                <ul class="navbar-nav me-auto">

                {% if app.user %}
                    <li class="nav-item dropdown" style="margin-right: 0rem">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.firstName }}</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Modifier mes informations</a>
                            <a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id}) }}">Modifier mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
                        </div>
                    </li>       
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('security.login') }}">Connexion
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('security.registration') }}">Inscription
                            </a>
                        </li>

                {% endif %}

                    
                </ul>
            </div>

        </div>
    </div>
</nav>
{# 6. Doctrine c'set un ORM object relational maping - se charge de registrement des donnés - ca permets de ne pas ecrire la base de donnes a la main - on gere tout depuis des objets php - c'est symfony qui va se charger de transformer tout ce qu'on a ecrit via des objets/classe php en SQL => symfony trasmets a la bd)  #}